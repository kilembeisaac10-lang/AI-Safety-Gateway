from setuptools import setup, find_packages

setup(
    name="ai-safety-gateway",
    version="1.0.0",
    description="Defensive AI Safety Gateway",
    packages=find_packages(),
    include_package_data=True,
)